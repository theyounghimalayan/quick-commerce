what is modal.tsx
why was footer absolute
why is token storage separate than details storage
why claude provided three functions tokenStorage, storage, and storage utility
how to create a splash screen in expo go
how to check network calls in expo go app or expo app without any android studio or xcode
using textarea
Argument of type '"/(delivery)"' is not assignable to parameter of type 'RelativePathString | ExternalPathString | "/modal" | `/modal?${string}` | `/modal#${string}` | "/../features/dashboard/ProductDashboard" | `/../features/dashboard/ProductDashboard?${string}` | ... 66 more ... | { ...; }'.ts(2345)
catching errors in expo
error boundaries and how do they work and how to use them
CORS using mobile
the interceptor refresh token logic and can it not be stuck in a loop, should it be returning Promise.resolve(error) or should it be returning Promise.reject(error);
Only using config in interceptors request (is there any other way, or is something missing)
geoLocation differences
routing despite error in the customerLogin function
mongo db disconnecting after sometime, and again and again adding an IP to mongo db (to the list of IPs which can access the database)
out of these <Stack>
        <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
        <Stack.Screen
          name="modal"
          options={{ presentation: "modal", headerShown: false }}
        />
        <Stack.Screen name="(auth)" options={{ headerShown: false }} />
        <Stack.Screen name="(delivery)" options={{ headerShown: false }} />
        <Stack.Screen name="(customer)" options={{ headerShown: false }} />
      </Stack> where will the layout navigate first, why does it navigate to the customer dashboard
check the text rendering error, how it was solved
going back on swipe even after navigation through router.replace
through new error if location permission is denied (how to handle this)
add custom fonts (the way it has been used in the grocery delivery app)
could not use svg because the react native svg package has version 15 which uses fabric which is not supported in expo Go (try using prebuild later)
Animated from react native vs animated from react native reanimated
features vs components (for dashboard we have files in both the folders)
flex 1 in notice animations in the container class is making context disappear
parallax causing padding, snapEnabled, pagingEnabled in carousel study these things
what is the initial route in the app in expo router with your current file structure
mess with aspect ratio in categories product image
why did he use runOnJS and why is it deprecated